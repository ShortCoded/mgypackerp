<!doctype html><html><head><meta charset="utf-8"><title>{{ $record->run_number }}</title><style>body{font-family:sans-serif;margin:32px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #999;padding:6px;text-align:left}</style></head><body onload="window.print()"><h2>{{ __('Production Traveler') }} — {{ $record->run_number }}</h2><p>{{ $record->order?->doc_num }} | {{ $record->product?->name }} | {{ $record->planned_start_at?->format('Y-m-d H:i') }} → {{ $record->planned_end_at?->format('Y-m-d H:i') }}</p><p>{{ __('Batch / lot') }}: {{ $record->batch_lot }}</p><table><thead><tr><th>{{ __('Material') }}</th><th>{{ __('Planned') }}</th><th>{{ __('Issued') }}</th><th>{{ __('Returned') }}</th><th>{{ __('Consumed') }}</th><th>{{ __('Waste') }}</th></tr></thead><tbody>@foreach($record->requirements as $line)<tr><td>{{ $line->product?->doc_num }} — {{ $line->product?->name }}</td><td>{{ $line->planned_quantity }} {{ $line->unit?->name }}</td><td>{{ $line->issued_quantity }}</td><td>{{ $line->returned_quantity }}</td><td>{{ $line->consumed_quantity }}</td><td>{{ $line->waste_quantity }}</td></tr>@endforeach</tbody></table></body></html>
